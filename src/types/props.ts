export interface ChildComponentProps {
  user?: string;
  type?: string;
}

export interface WrapperProps {}

export interface EmailStore {
  useremail: string;
}

export interface loginInputDatetype {
  id: string;
  label: string;
  name: string;
  title?: string;
  autoComplete: string;
  type?: string;
  autoFocus: string;
  helperText?: string;
}
